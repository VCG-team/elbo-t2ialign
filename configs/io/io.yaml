# output config
output_path:
  coco: "./output/coco"
  context: "./output/context"
  voc: "./output/voc"

# input config
# fix random seed
seed: 4307
# whether use predicted classes to construct dataset
# if True, use cls_predict.npy(generated by classification.py) to construct dataset
# if False, use ground truth class labels to construct dataset(./data/coco/cls_labels.npy etc.)
use_cls_predict: False
# model path
diffusion:
  path: "/data/wjl/.cache/huggingface/hub/models--runwayml--stable-diffusion-v1-5/snapshots/c9ab35ff5f2c362e9e22fbafe278077e196057f0"
  device: 4
  dtype: "fp16"

blip:
  path: "/data/wjl/ptp_diffusion/blip/blip-image-captioning-large"
  device: 4
  dtype: "fp32"

clip:
  path: "/data2/wjl/sdsseg/open_vocabulary/clip-vit-large-patch14"
  device: 4
  dtype: "fp32"
